name: Backend Workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-backend:
    name: Build & Test Backend
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: simple-todo-backend

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Dependencies
        run: npm install

      - name: Run Backend (Check if builds)
        run: |
          npm run build || echo "No build script defined"

      - name: Run Tests
        run: |
          npm test || echo "No tests defined"

      # Optional: Upload backend artifacts/logs
      # - name: Upload Backend Logs or Artifacts
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: backend-logs
      #     path: simple-todo-backend/logs
